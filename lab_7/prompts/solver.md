
        # Нижняя граница
        # а зачем инициализировать u на основе нижней грани, если задача нестационарная? или нач усл != гр усл?
        # или нач усл лишь означает зависимость от времени? а граничное условие - зависимость от пространственных узлов?
        # как понимать независимость задачи от времени? меняется лишь координата?
        # почему если задача двумерная, к ней применим МПИ? 
        # слау содержит n-4 уравнений относительно неизвестных, угловые узлы в вычислениях не участвуют. 

        u.append([self._start_bottom(self._xd*i) for i in range(self._nx+1)])

        # Всё кроме нижней и верхней границы
        for _ in range(1, self._ny):
            u.append([0]*(self._nx+1))

        # Верхняя граница
        u.append([self._start_top(self._xd*i) for i in range(self._nx+1)])

        # Левая и правая граница
        for j in range(self._ny+1):
            u[j][0] = self._start_left(self._yd*j)
            u[j][self._nx] = self._start_right(self._yd*j)

        # Лин. интерп. горизонтальных проекций гр. знач. на коорд. линиях (по вертикали между верхом и низом) для создания нач. приближения.
        # При True число итераций до сходимости сокращается.
        # Если j мал (близко к нижней границе), вес u_bottom_interpolated будет большим.
        # Если j велик (близко к верхней границе), вес u_top_interpolated будет большим.
        if (interpol == True):
            for j in range(1, self._ny):
                for i in range(1, self._nx):
                    u_bottom_interpolated = (u[0][0] * (self._nx+1-i) + u[0][self._nx] * i) / (self._nx + 1) # угловые точки нижней границы (лево и право, вдоль y = 0)
                    u_top_interpolated = (u[self._ny][0] * (self._nx+1-i) + u[self._ny][self._nx] * i) / (self._nx + 1) # угловые точки верхней границы (лево и право, вдоль y = Ly)
                    u[j][i] = (u_bottom_interpolated * (self._ny+1-j) + u_top_interpolated * j) / (self._ny + 1)  # создаем начальное приближение
