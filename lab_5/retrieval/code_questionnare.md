1. от чего зависит кол-во создаваемых файлов? 
2. на что влияет кол-во шагов в init?
3. в чем ценность конечного времени max_t? на что влияет? куда и когда оно подставляется?
4. куда подставляется max_t?
5. где шаги времени?
6. какова функция сетки?
7. как считается погрешность (где?) для данного времени?
- это модуль разности u и ts
    - а что такое u? что такое ts?

8. какие действия совершаются после шага решения _post_solution?
- что значит присвоить переменной нечто         cur_true = [self._true_sol(i*self._xd, t) for i in range (self._n+1)]


- верно ли я понимаю, что в питоне есть несколько версий списочной инициализации? например list(нечто) и [нечто] ?
а какие еще есть?
- верно ли понимаю, что погрешность сохраняется в соответствующей переменной в результате исполнения приватной ф-ции подсчета погрешности?
- как работает self._write_res? 
    - Записать время, вычисленное и точное значение для каждой точки в файл.
    - Нам бы стоило дополнить эту ф-цию? или создать отдельную для 2-д?
    - а почему у меня график параболы? это обусловлено гр. условиями? а у хоанга была как бы синусоида?

- поясни эту строку в составе функции cleanup_dir         txt_files = [f for f in os.listdir(self._path) if f.endswith('.txt')]


- в чем отличие param и cron_param?
- почему в первом типе схемы параметр не участвует? что это за схема? явная. там не преобразуются производные?
- явная схема не задает коэф? она решает напрямую? не совсем. а какие коэффициенты abcd задает неявная / кранка-николсона?
- правильно ли понимаю, что явная схема даже не использует метод прогонки? а как происходит решение?  на основе каких действий считается решение в явной схеме?

- обоснуй этот способ инициализации в python a = [0]*(self._n+1); b = [0]*(self._n+1); c = [0]*(self._n+1); d = [0]*(self._n+1)
это умножение 0 элемента списка на что?

- почему значения коэфф для каждой из схем аппроксимации отличаются?
- почему мы можем рассмотреть случай трех видов аппроксимации как для явной схемы, так и для неявной? почему это не противоречивое рассмотрение?
- как в коде отражено участие двух слоев: верхнего и нижнего, пространственного и временного?
- за что отвечает параметр схемы кранка николсона?
- опишите увиденный вами график.
- каков источник, смысл указанных коэффициентов         self._xd = math.pi / self._n, self._td = 0.5*self._xd**2
- 

- t_d временная размерность
- x_d пространственная размерность


- по каким формулам ты это программировал? покажи соответствие?
    - на основе чего выведены формулы конкретно для твоего варианта? покажи вывод этих формул / аналитическое решение?
