
Представь ты эксперт в python. 


## 1. Сделай код модульным. Разбей на функции, но не на классы. Раздели комментариями блоки программы.
## 2. Выполни сохранение графика в папку по аналогии
OUTPUT_DIRECTORY = '/home/snowwy/snap/MAI/_math/8_Численные_методы/numerical_methods/lab_5/src/plots'
```python
plt.tight_layout()
os.makedirs(output_dir, exist_ok=True)
graph_filepath = os.path.join(output_dir, filename)
plt.savefig(graph_filepath, dpi=300)
print(f"\nГрафик сохранен в файл {graph_filepath}")
```

## 3. график для каждого метода отдельно. 2d, 3d.


## 4. Внедри в этот код проверки:

1. откуда генерируются точки? 
2. что если поменять коэф у рунге-кутт? as, bs, cs? порядок точности? можем ли ограничить?

3. что если поменять интервал?

4. что если поменять начальные условия?

5. что если поменять шаг? 
6. явно выводи в консоль порядок точности для метода. на шаге и на интервале. 
7. проверка на обусловленность, свойства устойчивости и аппроксимации схемы
8. поменяй имена на корректные


мне необходимо также сделать 3д-визуализацию.